name: probot
description: Probot - Pro Sport Robots

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  font_awesome_flutter: ^8.5.0
  http: ^0.12.0+4
  flutter_svg: ^0.17.0
  shared_preferences: ^0.5.6+1

dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/background_logo.png
    - images/logo.png
    - images/logo_shadow.png
    - images/silueta-active.png
    - images/silueta-disabled.png
    - images/croatia-flag.png
    - images/dinamo.jpg
    - images/dd-silueta.png
    - images/kragna.png
    - images/dres.png
    - images/icons/logout.png
    - images/icons/role.png
    - images/icons/menu.png
    - images/icons/quick_kick.png
    - images/icons/season_shootout.png
    - images/icons/duel_designer.png
    - images/icons/difficulty_easy.png
    - images/icons/difficulty_medium.png
    - images/icons/difficulty_hard.png
    - images/icons/height.png
    - images/icons/jump_height.png
    - images/icons/batt.png
    - images/icons/player.png
    - images/icons/down.png
    - images/icons/substitute.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Barlow
      fonts:
        - asset: fonts/Barlow-Bold.otf
        - asset: fonts/Barlow-Medium.otf
        - asset: fonts/Barlow-SemiBold.otf
    - family: BarlowCondensed
      fonts:
        - asset: fonts/BarlowCondensed-Light.otf
        - asset: fonts/BarlowCondensed-Medium.otf
        - asset: fonts/BarlowCondensed-SemiBold.otf
  #         style: italic
    - family: ProbotIcons
      fonts:
        - asset: icons/ProbotIcons.ttf

    - family: ProbotParams
      fonts:
        - asset: icons/ProbotParams.ttf

    - family: ProbotTriangle
      fonts:
        - asset: icons/ProbotTriangle.ttf

    - family: BottomSheetIcons
      fonts:
        - asset: icons/BottomSheetIcons.ttf

    - family: TestSheetIcons
      fonts:
        - asset: icons/TestSheetIcons.ttf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
